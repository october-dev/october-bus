{
  "mcpServers": {
    "october_bus": {
      "command": "{{command}}",
      "args": [
        "mcp",
        "stdio",
        "--scope",
        "{{scope}}",
        "--agent",
        "{{agent}}",
        "--name",
        "{{name}}",
        "--data-dir",
        "{{dataDir}}",
        "--runtime-dir",
        "{{runtimeDir}}"
      ],
      "startupTimeoutMs": 30000,
      "toolTimeoutMs": 60000
    }
  }
}
